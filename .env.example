# Lokalny/Neon Postgres — dla Neon użyj połączonego (pooled) adresu z `-pooler` w nazwie hosta
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# Współdzielone hasło do sekcji admina (Skryptorium) — ustaw własne, silne hasło
ADMIN_PASSCODE="zmien-mnie-na-dlugie-haslo"
